Ont= understanding that the attachment was a gz file-pcapng.gz is it was compressed using the gzip compression algorithm.
So i download an open shource packet analyser -Wireshark
ON using dns as the filter got the flag.
