Since it was given in the descryption to use basic linux command.
The quest name itself suggested to use the grep command- which was used to find specific wordings inside the file we are opening up in the directory.
Hence the flag was captured.
